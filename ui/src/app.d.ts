// Ambient declarations.
//
// Vite injects asset and env types via `vite/client`; we only need to
// add Tauri-specific globals here when we use them.
/// <reference types="svelte" />
/// <reference types="vite/client" />

export {};
