# Rust / Cargo
target/
**/*.rs.bk
# Cargo.lock is committed: this workspace produces a binary
# application (qobee-app), so reproducible builds matter.

# Tauri build output
src-tauri/target/
src-tauri/gen/schemas/

# Local dev override that disables the beforeDevCommand hook;
# only meaningful on the maintainer's box where `npm --prefix ../ui`
# resolves wrong because of a parent-folder name shadowing.
src-tauri/tauri.conf.dev-override.json

# Node / Vite
node_modules/
ui/dist/
ui/.vite/
ui/.svelte-kit/

# IDE / OS
.vscode/
.idea/
.kiro/
*.swp
.DS_Store
Thumbs.db

# Logs
*.log

# Environment / secrets
.env
.env.local

# Tauri updater signing keys — the PRIVATE key must never be
# committed. It lives only on the maintainer's box and in the
# `TAURI_SIGNING_PRIVATE_KEY` CI secret. The matching PUBLIC key is
# embedded in `tauri.conf.json` (safe to commit) so shipped binaries
# can verify update signatures.
src-tauri/.tauri/
*.key

# Qobee runtime data (created by app at runtime, never committed)
qobee-data/
